# Inventory Module Improvements for Comprehensive Data Collection

## Tasks
- [x] Update inventory/models.py: Add new fields to Product, InventoryRecord, and StockMovement models
- [x] Update inventory/forms.py: Include new fields in forms
- [x] Run Django migrations to apply model changes (Note: Migration created but application failed due to DB connection issue - ensure PostgreSQL is running and DB exists)
- [x] Test forms and views for new data entry
- [x] Update templates if needed for new fields (e.g., image upload)
- [x] Update dashboard/export logic to include new fields if required
