# TODO: Replace store ID with Store Number in Store List Template

- [x] Edit `store_management/templates/store_management/store_list.html` to change "ID" column to "Store Number" and display `store.store_number` instead of `store.id`
